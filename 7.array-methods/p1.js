// instance methods
        // storing and retriving values
        // push(store) and pop(retrive)

        let user=[];
        console.log(`user=`,user)
        //last in

        user.push("john")
        console.log(`user=`,user)

        user.push("john")
        console.log(`user=`,user)

        user.push("john")
        console.log(`user=`,user)

        //last out
        console.log(`pop=`, user.pop())
        console.log(`user=`,user)

        console.log(`pop=`, user.pop())
        console.log(`user=`,user)
        
        console.log(`pop=`, user.pop())
        console.log(`user=`,user)
       